<input 
type="{{type}}" 
name="{{name}}"
value="{{value}}"
placeholder="{{placeholder}}" 
{{#if className}}class="{{className}}"{{/if}} 
{{#if validationType}}data-validation="{{ validationType }}" {{/if}} 
/>
